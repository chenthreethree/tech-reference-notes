# 第一部分：Python 基础与常见数据结构

重点整理深浅拷贝、字典、函数、可变对象等常见知识点。

1. 问题：Python 是什么？

> 答案：Python 是一种高级、解释型、通用编程语言。它语法相对简洁，拥有丰富的科学计算和深度学习生态，例如 NumPy、PyTorch、Pandas。所谓解释型，是指代码通常由解释器在运行时执行，而不是先整体编译成机器程序。

2. 问题：Python 中常见的数据类型有哪些？

> 答案：常见类型包括：整数 int、浮点数 float、布尔值 bool、字符串 str、列表 list、元组 tuple、字典 dict、集合 set。其中列表、字典和集合通常是可变对象；字符串、数字和元组通常是不可变对象。

3. 问题：列表和元组有什么区别？

> 答案：列表使用 \[\]，可以增加、删除或修改元素，是可变序列；元组使用 ()，创建后通常不能修改，是不可变序列。需要频繁修改时用列表；希望数据固定、可作为字典键时可考虑元组。

4. 问题：字典是什么？

> 答案：字典是由键值对组成的数据结构，例如 {"name": "示例用户"}。通过键访问值，平均查找、插入和删除复杂度通常为 O(1)。键必须是可哈希对象，例如字符串、数字或只含不可变元素的元组。

5. 问题：Python 字典底层是什么结构？

> 答案：Python 字典底层主要基于哈希表。系统先对键计算哈希值，再根据哈希值寻找存储位置。不同键可能产生冲突，Python 会使用内部探测机制寻找其他位置。

6. 问题：“字典的深层次结构”可能指什么？

> 答案：第一种含义是嵌套字典，即字典的值仍然是字典或列表，例如学生信息中包含教育经历和论文列表。访问时可写 student\["education"\]\["school"\]。
> 
> 第二种含义是字典底层实现，即哈希表、哈希值和冲突处理。遇到表述不明确时，可以先确认：“这里指的是嵌套结构，还是字典的底层实现？”

7. 问题：什么是浅拷贝？

> 答案：浅拷贝会创建一个新的外层对象，但内部嵌套的可变对象仍然与原对象共享引用。字典可以使用 a.copy()，也可以使用 copy.copy(a)。修改第一层通常不影响原对象，但修改内部列表或字典可能影响原对象。

8. 问题：什么是深拷贝？

> 答案：深拷贝会递归复制对象内部的各层可变结构，使新对象与原对象基本独立。使用 copy.deepcopy(a)。修改深层列表或字典时，通常不会影响原对象。

9. 问题：直接赋值、浅拷贝和深拷贝有什么区别？

> 答案：b = a：没有创建新对象，a 和 b 指向同一对象。
> 
> b = a.copy()：创建新外层，但内部可变对象仍共享。
> 
> b = copy.deepcopy(a)：递归复制各层对象，深层修改通常不互相影响。

10. 问题：什么是可变对象和不可变对象？

> 答案：可变对象创建后可以原地修改，例如列表、字典、集合；不可变对象创建后不能原地改变，例如整数、浮点数、字符串和元组。不可变不是“变量不能重新赋值”，而是原对象本身不能被修改。

11. 问题：\`is\` 和 \`==\` 有什么区别？

> 答案：== 比较两个对象的值是否相等；is 比较两个变量是否指向同一个对象。判断是否为 None 时通常使用 is None。

12. 问题：函数参数中的可变默认值有什么风险？

> 答案：例如 def f(x=\[\]): 中的列表只在函数定义时创建一次，后续调用会复用同一个列表，可能产生意外累积。更安全的写法是默认值设为 None，在函数内部再创建列表。

13. 问题：什么是局部变量和全局变量？

> 答案：函数内部创建的变量通常是局部变量，只在函数作用域内有效；函数外定义的变量通常是全局变量。函数内若要直接修改全局变量，需要使用 global，但工程中通常应尽量减少全局状态。

14. 问题：异常处理有什么作用？

> 答案：异常处理使用 try-except 捕获运行时错误，避免程序直接终止，并给出可控处理。finally 中的代码无论是否发生异常通常都会执行。应尽量捕获具体异常，而不是无条件捕获所有异常。

15. 问题：Python 中的 \`with\` 有什么作用？

> 答案：with 用于上下文管理，可以在代码块结束时自动释放资源。例如 with open(...) as f 会自动关闭文件。深度学习中也常见 with torch.no\_grad():，用于临时关闭梯度记录。

16. 问题：列表推导式是什么？

> 答案：列表推导式是一种简洁的列表生成方式，例如 \[x\*x for x in nums if x \> 0\]。它适合简单转换；逻辑复杂时应改用普通循环，避免影响可读性。

17. 问题：时间复杂度 O(1)、O(n)、O(n²) 分别是什么意思？

> 答案：O(1) 表示数据规模增大时操作次数基本不变；O(n) 表示操作量与数据规模近似成正比；O(n²) 常见于双重循环。复杂度描述的是增长趋势，不是精确运行时间。

# 第二部分：NumPy、PyTorch 与张量操作

先理解 PyTorch 是什么，再掌握数组、张量、维度、转置、设备、自动求导和训练流程。

18. 问题：PyTorch 是什么？

> 答案：PyTorch 是一个主要用于深度学习和张量计算的开源框架。它提供类似 NumPy 的张量运算，同时支持 GPU 加速、自动求导、神经网络模块和优化器。
> 
> 可以把它理解为：NumPy 主要解决通用数值计算；PyTorch 在张量计算基础上，进一步提供了训练神经网络所需的一整套工具。

19. 问题：PyTorch 主要包含哪些核心能力？

> 答案：torch.Tensor：保存和计算多维数据。
> 
> autograd：自动计算梯度。
> 
> torch.nn：构建神经网络层和损失函数。
> 
> torch.optim：提供 SGD、Adam、AdamW 等优化器。
> 
> Dataset 和 DataLoader：组织数据和按批读取。
> 
> CUDA 支持：将张量和模型放到 GPU 上加速。

20. 问题：NumPy 和 PyTorch 有什么区别？

> 答案：NumPy 的主要数据结构是 ndarray，通常用于 CPU 数值计算，不负责神经网络自动求导。
> 
> PyTorch 的主要数据结构是 Tensor，可以在 CPU 或 GPU 上运行，并能通过自动求导参与模型训练。两者许多 API 形式相似，但对象类型和运行机制不同。

21. 问题：数组和张量有什么区别？

> 答案：在日常深度学习语境中，数组通常指 NumPy 的 ndarray，张量通常指 PyTorch 的 Tensor。张量可以是一维向量、二维矩阵或更高维数据，并且能够放在 GPU 上、记录梯度。

22. 问题：标量、向量、矩阵和张量是什么？

> 答案：标量是单个数，例如 3.2；向量是一维数据；矩阵是二维数据；张量是更一般的多维数据结构。
> 
> 在深度学习中，标量、向量和矩阵都可以看成张量的特殊情况。

23. 问题：如何把 NumPy 数组转换为 PyTorch 张量？

> 答案：可以使用 torch.from\_numpy(array)；反向转换可以使用 tensor.cpu().numpy()。如果张量在 GPU 上，需要先 .cpu()；如果正在记录梯度，通常还要先 .detach()。

24. 问题：什么是张量的 shape？

> 答案：shape 表示各个维度的大小。例如形状 (32, 3, 224, 224) 常表示一个 batch 中有 32 张图，每张图有 3 个通道，高和宽都是 224。

25. 问题：\`224 × 224 × 3\` 表示什么？

> 答案：表示一张彩色图像的高、宽和通道数：高度 224 个像素，宽度 224 个像素，3 个颜色通道。
> 
> 这 3 个通道通常是红色 R、绿色 G、蓝色 B。不同库的维度顺序可能不同：图像文件或 NumPy 常见 H×W×C，PyTorch 常见 C×H×W。

26. 问题：为什么 PyTorch 图像常写成 \`3 × 224 × 224\`？

> 答案：PyTorch 卷积网络通常采用 CHW 顺序，即通道、高、宽；加入 batch 后通常是 NCHW，例如 (32, 3, 224, 224)。而 NumPy 或图像库常使用 HWC，需要通过转置或 permute 调整。

27. 问题：矩阵转置是什么？

> 答案：矩阵转置就是交换行和列。一个形状为 m×n 的矩阵转置后变为 n×m。NumPy 中可用 A.T 或 np.transpose(A)；PyTorch 二维张量可用 A.T 或 A.transpose(0, 1)。

28. 问题：PyTorch 的 \`transpose\` 和 \`permute\` 有什么区别？

> 答案：transpose(dim0, dim1) 只交换两个指定维度；permute(...) 可以一次重新排列所有维度。例如把 HWC 转成 CHW，可以写 x.permute(2, 0, 1)。

29. 问题：转置和求逆有什么区别？

> 答案：转置是交换行列，对任意矩阵都可以进行；求逆是寻找满足 A·A⁻¹=I 的矩阵，通常要求 A 是可逆方阵。转置用 .T；求逆用 np.linalg.inv() 或 torch.linalg.inv()。

30. 问题：\`np.linalg.inv()\` 和 \`torch.linalg.inv()\` 有什么区别？

> 答案：np.linalg.inv() 接收 NumPy 数组，通常在 CPU 上执行，不参与深度学习自动求导；torch.linalg.inv() 接收 PyTorch 张量，可在 CPU/GPU 上执行，并支持自动求导。它们完成的数学操作相同，但所属框架和能力不同。

31. 问题：为什么求解 \`Ax=b\` 时常不用显式求逆？

> 答案：直接计算 A⁻¹b 往往计算量更大、数值稳定性更差。通常使用 np.linalg.solve(A, b) 或 torch.linalg.solve(A, b) 直接求解。

32. 问题：\`reshape\`、\`view\` 和 \`flatten\` 分别是什么？

> 答案：reshape 改变张量形状，必要时可能生成新内存；view 通常要求内存连续，只改变查看方式；flatten 将若干维合并成一维。
> 
> 它们都不应改变元素总数。例如 (2,3,4) 可以变成 (6,4)，但不能无故变成元素总数不同的形状。

33. 问题：什么是 broadcasting（广播）？

> 答案：广播允许不同形状的张量在满足规则时进行逐元素运算。例如 (32, 3, 224, 224) 可以减去形状为 (1, 3, 1, 1) 的通道均值。系统会在大小为 1 的维度上虚拟扩展数据。

34. 问题：矩阵乘法和逐元素乘法有什么区别？

> 答案：矩阵乘法遵循行乘列规则，PyTorch 中常用 @ 或 torch.matmul()；逐元素乘法要求形状相同或可广播，使用 \*。二者结果和含义不同。

35. 问题：什么是 device？

> 答案：device 表示张量或模型所在的计算设备，常见是 CPU 或 CUDA GPU。模型和输入数据必须位于同一设备，通常写 device = torch.device("cuda" if torch.cuda.is\_available() else "cpu")。

36. 问题：什么是自动求导 autograd？

> 答案：PyTorch 会记录张量参与的运算图。当损失调用 loss.backward() 时，系统根据链式法则自动计算各参数的梯度，并存入参数的 .grad。

37. 问题：\`requires\_grad=True\` 是什么意思？

> 答案：表示需要跟踪该张量相关的运算，以便之后计算梯度。神经网络的可训练参数通常自动开启；输入数据一般不需要，除非研究输入梯度或对抗样本。

38. 问题：为什么每轮训练前要 \`optimizer.zero\_grad()\`？

> 答案：PyTorch 默认会累积梯度。如果不清零，本轮梯度会与前面批次相加。标准训练流程通常在反向传播前调用 zero\_grad()。

39. 问题：\`model.train()\` 和 \`model.eval()\` 有什么区别？

> 答案：model.train() 切换到训练模式，Dropout 和 BatchNorm 按训练方式工作；model.eval() 切换到评估模式，Dropout 关闭，BatchNorm 使用已统计的均值和方差。它们不会自动开启或关闭梯度。

40. 问题：\`torch.no\_grad()\` 有什么作用？

> 答案：在代码块中关闭梯度记录，减少显存和计算开销，常用于验证和推理。它与 model.eval() 作用不同：前者控制梯度，后者控制层的运行模式。

41. 问题：PyTorch 的基本训练流程是什么？

> 答案：将模型和数据放到设备上；前向传播得到输出；计算损失；梯度清零；反向传播；优化器更新参数。
> 
> 核心顺序通常是：optimizer.zero\_grad() → output=model(x) → loss=criterion(output,y) → loss.backward() → optimizer.step()。

42. 问题：什么是 \`nn.Module\`？

> 答案：nn.Module 是 PyTorch 中所有神经网络模块的基类。自定义模型通常继承它，在 \_\_init\_\_ 中定义层，在 forward 中描述前向计算。

43. 问题：什么是 \`state\_dict\`？

> 答案：state\_dict 是包含模型参数或优化器状态的字典。保存模型通常使用 torch.save(model.state\_dict(), path)，加载时先建立同结构模型，再调用 load\_state\_dict()。

# 第三部分：线性代数、概率与优化基础

这些概念是理解注意力、损失函数、相似度和模型训练的基础。

44. 问题：向量点积是什么？

> 答案：两个同维向量对应元素相乘后求和。点积可以反映向量方向的一致程度，也是矩阵乘法和注意力相似度计算的基础。

45. 问题：余弦相似度是什么？

> 答案：余弦相似度用两个向量夹角的余弦衡量方向相似性：x·y / (||x|| ||y||)。越接近 1 通常表示方向越相似。CLIP 常用它比较图像特征和文本特征。

46. 问题：什么是范数？

> 答案：范数用于度量向量大小。常见 L1 范数是绝对值之和，L2 范数是平方和开根号。对抗攻击中常用范数限制扰动大小。

47. 问题：什么是梯度？

> 答案：梯度由函数对各变量的偏导数组成，表示函数值增长最快的方向。训练时通常沿负梯度方向更新参数以减小损失。

48. 问题：什么是链式法则？

> 答案：当一个函数由多层函数复合而成时，整体导数等于各层局部导数按依赖关系相乘。反向传播就是在计算图上高效应用链式法则。

49. 问题：Softmax 是什么？

> 答案：Softmax 将一组实数转换为和为 1 的非负概率分布。较大的输入会得到较大的概率。分类输出和注意力权重中都常使用 Softmax。

50. 问题：为什么注意力中要除以 \`sqrt(d\_k)\`？

> 答案：Q 和 K 的维度较大时，点积可能过大，使 Softmax 进入非常饱和的区域、梯度变小。除以 sqrt(d\_k) 可以控制数值尺度，使训练更稳定。

51. 问题：什么是概率分布？

> 答案：概率分布描述随机变量不同取值出现的可能性。分类模型输出的各类别概率通常和为 1；生成模型则学习数据的分布并从中采样。

52. 问题：交叉熵损失是什么？

> 答案：交叉熵衡量预测概率分布与真实标签分布之间的差异。对正确类别给出的概率越高，损失通常越小。多分类任务常使用 CrossEntropyLoss。

53. 问题：均方误差 MSE 是什么？

> 答案：MSE 是预测值与真实值差的平方的平均值，常用于回归和重建任务。平方会对较大误差施加更强惩罚。

54. 问题：什么是梯度下降？

> 答案：梯度下降根据损失函数对参数的梯度更新参数：参数减去学习率乘梯度。实际深度学习通常使用小批量随机梯度下降及其变体。

55. 问题：学习率过大或过小分别会怎样？

> 答案：学习率过大可能导致震荡、发散或越过最优点；过小会导致收敛很慢，也可能在有限训练时间内达不到较好结果。

# 第四部分：机器学习与实验评估基础

重点理解训练、验证、测试、过拟合、类别不平衡和常见评价指标。

56. 问题：监督学习、无监督学习和自监督学习有什么区别？

> 答案：监督学习使用带标签数据学习输入到标签的映射；无监督学习没有人工标签，常用于聚类或异常检测；自监督学习从数据本身构造训练目标，例如遮挡预测或对比学习。

57. 问题：分类和回归有什么区别？

> 答案：分类预测离散类别，例如真图或假图；回归预测连续数值，例如价格或风险分数。两类任务通常使用不同输出层、损失函数和评价指标。

58. 问题：训练集、验证集和测试集分别做什么？

> 答案：训练集用于更新参数；验证集用于选择模型、阈值和超参数；测试集只用于最终客观评估。不能根据测试集反复调参，否则会造成测试信息泄漏。

59. 问题：什么是过拟合？

> 答案：模型在训练集表现很好，但在未见数据上明显变差，说明模型记住了训练细节而没有学到可泛化规律。可通过数据增强、正则化、早停、更多数据等方式缓解。

60. 问题：什么是欠拟合？

> 答案：模型在训练集和验证集上都表现较差，通常说明模型能力不足、训练不充分、特征不足或优化设置不合适。

61. 问题：什么是偏差与方差？

> 答案：高偏差通常对应模型过于简单、欠拟合；高方差通常对应模型对训练数据过度敏感、过拟合。模型设计需要在二者之间平衡。

62. 问题：什么是数据泄漏？

> 答案：训练过程使用了本不应获得的测试信息，例如用测试集选择阈值、同一人的相似样本同时出现在训练和测试中。数据泄漏会使结果虚高。

63. 问题：准确率 ACC 是什么？

> 答案：ACC 是预测正确样本数占总样本数的比例。类别极度不平衡时，准确率可能具有误导性，例如全部预测为多数类仍可得到很高 ACC。

64. 问题：Precision 和 Recall 分别是什么？

> 答案：Precision 表示预测为正的样本中有多少是真的正样本；Recall 表示所有真实正样本中有多少被检测出来。安全检测中二者通常需要权衡。

65. 问题：F1 分数是什么？

> 答案：F1 是 Precision 与 Recall 的调和平均，只有两者都较高时 F1 才高。适合类别不平衡、同时重视误报和漏报的任务。

66. 问题：TPR 和 FPR 分别是什么？

> 答案：TPR 等于真正例率，也就是 Recall；FPR 表示真实负样本中被错误判断为正样本的比例。水印检测常报告 TPR@1%FPR。

67. 问题：ROC 曲线和 AUC 是什么？

> 答案：ROC 曲线展示不同阈值下 TPR 与 FPR 的关系；AUC 是 ROC 曲线下面积，衡量模型跨阈值的整体排序能力。AUC 越高，一般表示正样本更容易排在负样本前面。

68. 问题：PR 曲线和 AP 是什么？

> 答案：PR 曲线展示不同阈值下 Precision 与 Recall 的关系；AP 可理解为 PR 曲线的综合面积指标。类别不平衡时，AP 往往比 Accuracy 或 ROC-AUC 更有解释力。

69. 问题：阈值有什么作用？

> 答案：模型常先输出连续分数，阈值决定最终正负分类。降低阈值通常提高召回率但增加误报；提高阈值通常减少误报但可能增加漏报。

70. 问题：类别不平衡怎么处理？

> 答案：可以使用重采样、类别权重、Focal Loss、阈值调整、合适的评价指标以及收集更多少数类数据。不能只看准确率。

71. 问题：什么是交叉验证？

> 答案：将数据划分为多个折，轮流用一折验证、其余训练，最终汇总结果。它能更稳定地估计小数据集上的性能，但计算成本更高。

72. 问题：Baseline 是什么？

> 答案：Baseline 是用于比较的基准方法，可以是简单规则、已有方法或不含关键创新的模型。新方法必须与合理基线比较，才能说明改进是否有效。

73. 问题：消融实验是什么？

> 答案：消融实验通过移除或替换方法中的某个模块，观察性能变化，从而验证每个设计的实际贡献。

74. 问题：泛化能力是什么？

> 答案：泛化能力是模型在未见数据、不同数据集、不同生成器或不同攻击条件下保持性能的能力。训练集结果高并不等于泛化好。

# 第五部分：深度学习训练与常见网络模块

理解神经网络如何前向计算、反向传播和更新参数。

75. 问题：神经网络中的参数是什么？

> 答案：参数是训练过程中由数据学习的数值，例如线性层和卷积层的权重与偏置。超参数则由研究者设定，例如学习率、batch size 和层数。

76. 问题：前向传播和反向传播是什么？

> 答案：前向传播把输入依次通过各层得到预测和损失；反向传播从损失出发，根据链式法则计算每个参数的梯度。优化器再根据梯度更新参数。

77. 问题：Batch、Iteration 和 Epoch 有什么区别？

> 答案：Batch 是一次送入模型的一小批样本；Iteration 是使用一个 batch 完成一次参数更新；Epoch 表示整个训练集被完整遍历一次。

78. 问题：激活函数有什么作用？

> 答案：激活函数引入非线性，使多层网络能够拟合复杂关系。若每层都只有线性变换，多层叠加仍然等价于一个线性变换。

79. 问题：ReLU 是什么？

> 答案：ReLU 定义为 max(0,x)，计算简单、缓解部分梯度消失问题，因此广泛使用。缺点是负区间梯度为零，可能出现“神经元死亡”。

80. 问题：Dropout 是什么？

> 答案：训练时随机将部分神经元输出置零，降低神经元之间的过度依赖，从而缓解过拟合。推理时关闭 Dropout，并按训练规则进行尺度处理。

81. 问题：BatchNorm 和 LayerNorm 有什么区别？

> 答案：BatchNorm 通常按 batch 统计某个通道的均值和方差，依赖 batch 统计，常用于 CNN。
> 
> LayerNorm 通常对单个样本内部的特征维进行归一化，不依赖其他样本，适合 Transformer 和变长序列。

82. 问题：SGD、Adam 和 AdamW 有什么区别？

> 答案：SGD 使用当前梯度更新，常结合动量；Adam 同时维护梯度的一阶矩和二阶矩，为不同参数自适应调整步长；AdamW 将权重衰减与梯度更新解耦，在 Transformer 训练中非常常见。

83. 问题：L2 正则化和权重衰减是什么？

> 答案：L2 正则化在目标中惩罚过大的权重；权重衰减直接在参数更新时缩小权重。对普通 SGD 二者常可等价理解，但在 Adam 中解耦后的 AdamW 更清晰。

84. 问题：早停 Early Stopping 是什么？

> 答案：当验证集性能连续若干轮不再提升时停止训练，避免继续拟合训练集并节省计算资源。

85. 问题：CNN 是什么？

> 答案：CNN 是卷积神经网络，通过卷积核在局部区域共享参数提取空间特征。浅层常学习边缘和纹理，深层逐渐学习更复杂语义。

86. 问题：卷积层中的卷积核是什么？

> 答案：卷积核是一个小尺寸可学习权重矩阵，在输入上滑动，对局部区域做加权求和。不同卷积核可以学习不同模式。

87. 问题：池化层有什么作用？

> 答案：池化在局部区域做最大值或平均值汇总，用于降低空间尺寸、减少计算并提高一定的平移稳定性。现代网络也常通过步幅卷积替代部分池化。

# 第六部分：Transformer 核心原理

这是本次交流最需要重点补齐的部分。

88. 问题：Transformer 是什么？

> 答案：Transformer 是一种以注意力机制为核心的神经网络架构。它不依赖循环逐步处理序列，可以并行计算，并通过自注意力直接建模任意位置之间的关系。
> 
> 原始 Transformer 包含 Encoder 和 Decoder；BERT 主要使用 Encoder，GPT 主要使用带因果掩码的 Decoder。

89. 问题：Token 是什么？

> 答案：Token 是模型处理序列时的基本单位。文本中可以是词、子词或字符；ViT 中是图像 Patch 经线性映射后的向量；离散图像生成模型中也可以是码本索引。

90. 问题：Embedding 是什么？

> 答案：Embedding 将离散 Token 或原始输入映射成连续向量，使模型可以进行数值计算。向量维度常称为隐藏维度或 embedding dimension。

91. 问题：为什么需要位置编码？

> 答案：纯自注意力本身不知道 Token 的顺序。位置编码向每个 Token 注入位置信息，使模型能区分第一个、第二个或不同图像区域。

92. 问题：Self-Attention 是什么？

> 答案：Self-Attention 让序列中的每个 Token 根据与其他 Token 的相关性，聚合其他位置的信息。
> 
> 每个输入通过线性变换生成 Q、K、V。Q 与所有 K 计算相似度，经缩放和 Softmax 得到权重，再对 V 加权求和。

93. 问题：Q、K、V 分别是什么？

> 答案：Query 表示当前 Token 想寻找什么信息；Key 表示每个 Token 提供的匹配特征；Value 表示真正被加权汇总的内容。
> 
> 三者都由输入向量乘不同的可学习矩阵得到，不是人工固定的数据。

94. 问题：Scaled Dot-Product Attention 的流程是什么？

> 答案：先计算 QKᵀ 得到相关性分数，再除以 sqrt(d\_k) 控制数值范围，经过 Softmax 得到权重，最后与 V 相乘得到加权结果。

95. 问题：多头自注意力是什么？

> 答案：多头注意力把特征维度分成多个头，每个头使用独立的 Q、K、V 投影，在不同子空间学习关系。
> 
> 不同头可能关注局部关系、远距离依赖或不同语义模式。各头结果拼接后再经过线性映射。

96. 问题：为什么要使用多个注意力头？

> 答案：单个头只能在一个表示子空间中计算关系；多个头可以同时学习多种互补关系，提高模型表达能力。头越多并不一定越好，还要考虑维度、计算量和冗余。

97. 问题：前馈网络 FFN 是什么？

> 答案：FFN 是对每个 Token 独立且使用相同参数的两层全连接网络，通常先升维、经过激活函数，再降回隐藏维度。
> 
> 注意力负责不同 Token 之间的信息交互，FFN 负责对每个 Token 的特征做非线性变换。

98. 问题：残差连接是什么？

> 答案：残差连接把子层输入直接加到子层输出上，即 y = x + F(x)。它为信息和梯度提供直接通路，缓解深层网络训练困难，并保留原始特征。

99. 问题：LayerNorm 是什么？

> 答案：LayerNorm 对单个样本中某个 Token 的特征维计算均值和方差并归一化，再通过可学习缩放和平移恢复表达能力。它有助于稳定数值分布和训练。

100. 问题：残差连接和 LayerNorm 的作用有什么区别？

> 答案：残差连接负责保留原信息并改善梯度传播；LayerNorm 负责稳定特征尺度和分布。二者经常组合使用，但解决的问题不同。

101. 问题：Transformer Encoder 一层通常包含什么？

> 答案：通常包含多头自注意力、残差连接、LayerNorm、前馈网络、再次残差连接和 LayerNorm。不同模型可能使用 Pre-Norm 或 Post-Norm 顺序。

102. 问题：Encoder 和 Decoder 有什么区别？

> 答案：Encoder 通常允许每个 Token 看到完整输入；自回归 Decoder 使用因果掩码，只允许当前位置看到之前的 Token，并可能通过交叉注意力读取 Encoder 输出。

103. 问题：什么是 Mask？

> 答案：Mask 用于屏蔽不应参与注意力的位置。Padding Mask 屏蔽补齐元素；Causal Mask 屏蔽未来 Token，保证自回归生成时不能偷看后文。

104. 问题：Transformer 相比 RNN 的主要优势是什么？

> 答案：Transformer 可以并行处理所有位置，长距离依赖路径更短；RNN 按时间步顺序计算，不易并行，长序列中梯度传播更困难。

105. 问题：Transformer 的主要缺点是什么？

> 答案：标准自注意力对序列长度的时间和显存复杂度通常为 O(n²)，长序列成本较高；模型通常还需要较多数据和计算资源。

# 第七部分：Vision Transformer（ViT）

重点理解图像尺寸、Patch、Token、CLS Token 和 ViT 与普通 Transformer 的关系。

106. 问题：Vision Transformer 是什么？

> 答案：ViT 是把 Transformer Encoder 应用于图像任务的模型。它将图像切成多个 Patch，把每个 Patch 转换为 Token，再加入位置编码，通过自注意力提取全局图像特征。

107. 问题：Patch 和 Token 的关系是什么？

> 答案：Patch 是图像中的原始小块；Patch 展平并经过线性映射后得到的向量叫 Patch Token。因此 Patch 是图像区域，Token 是该区域的数值表示。

108. 问题：一张 \`224×224×3\` 图像使用 \`16×16\` Patch，会得到多少 Patch？

> 答案：高和宽分别有 224/16=14 个 Patch，因此共有 14×14=196 个 Patch。每个 Patch 含 16×16×3=768 个原始数值，之后映射到模型隐藏维度。

109. 问题：ViT-L/14 中的 L 和 14 分别表示什么？

> 答案：L 表示 Large 规模版本；14 表示 Patch 边长通常为 14，即每个 Patch 是 14×14。对 224×224 图像，可得到 16×16=256 个 Patch。

110. 问题：CLS Token 是什么？

> 答案：CLS Token 是额外加入序列开头的可学习 Token。经过多层注意力后，它聚合全局信息，分类任务中常用最终 CLS 表示输入给分类头。并非所有视觉任务都必须使用 CLS Token。

111. 问题：Patch Embedding 是怎么实现的？

> 答案：可以先把 Patch 展平后接线性层；也可以使用卷积核大小等于 Patch 大小、步幅也等于 Patch 大小的卷积，一次完成切块和线性投影。

112. 问题：ViT 和 Transformer 的关系是什么？

> 答案：Transformer 是通用架构；ViT 是它在视觉领域的具体应用。ViT 改变了输入形式：把文本 Token 换成图像 Patch Token，主体仍然使用 Transformer Encoder。

113. 问题：ViT 和 CNN 的主要区别是什么？

> 答案：CNN 通过局部卷积和参数共享提取特征，具有较强局部归纳偏置；ViT 通过自注意力直接建模 Patch 之间的全局关系。
> 
> CNN 在中小数据上通常更容易训练；ViT 往往依赖大规模预训练，但扩展性和全局建模能力较强。

114. 问题：ViT 中每层 Token 数量会变化吗？

> 答案：标准 ViT 的 Encoder 通常保持 Token 数量不变，变化的是每个 Token 的表示。某些层次化视觉 Transformer 会逐步合并 Patch，使空间分辨率下降。

115. 问题：ViT 为什么还需要位置编码？

> 答案：Patch Token 形成序列后，自注意力仍然不知道每个 Patch 位于图像的哪个位置。位置编码用于保留空间顺序和相对位置。

# 第八部分：CLIP 与多模态学习

理解图像编码器、文本编码器、对比学习、零样本分类和 CLIP Score。

116. 问题：CLIP 是什么？

> 答案：CLIP 是一种图文对比预训练模型，由图像编码器和文本编码器组成。它把图像和文本映射到同一个特征空间，使匹配图文相近、不匹配图文远离。
> 
> CLIP 本身主要用于表征和匹配，不是直接生成图像的模型。

117. 问题：CLIP 的图像编码器和文本编码器分别做什么？

> 答案：图像编码器把图像转换成图像特征向量，可使用 ResNet 或 ViT；文本编码器通常使用 Transformer，把文本转换成文本特征向量。

118. 问题：CLIP 是怎么训练的？

> 答案：在一个 batch 中计算所有图像与所有文本的相似度。正确配对位于相似度矩阵对角线，训练目标是提高正确配对相似度，同时降低错误配对相似度。

119. 问题：什么是对比学习？

> 答案：对比学习通过“拉近正样本对、拉远负样本对”学习表示。CLIP 中正确图文是正样本对，批次中的其他图文组合通常充当负样本对。

120. 问题：CLIP 为什么能做零样本分类？

> 答案：先把类别写成文本提示，例如 “a photo of a cat”，分别计算图片与各类别文本的相似度，选择最高者作为预测。无需针对当前类别重新训练传统分类头。

121. 问题：CLIP 和 ViT 有什么关系？

> 答案：ViT 可以作为 CLIP 的图像编码器。CLIP-ViT-L/14 表示 CLIP 使用 ViT-L/14 提取图像特征；CLIP 还包含文本编码器和图文对比训练目标。

122. 问题：CLIP Score 是什么？

> 答案：CLIP Score 通常基于图像特征与文本特征的相似度，衡量图像和文本提示的语义一致性。分数高表示语义更匹配，但不保证图像一定清晰或无失真。

124. 问题：CLIP 有哪些局限？

> 答案：它可能继承训练数据偏差；对计数、细粒度空间关系和复杂逻辑理解有限；相似度高只代表语义匹配，不等同于真实感、清晰度或安全性。

# 第九部分：计算机视觉、生成模型与质量指标

覆盖图像通道、预处理、离散 Token 和常见图像指标。

125. 问题：RGB 三个通道分别是什么？

> 答案：R、G、B 分别表示红、绿、蓝三个颜色分量。每个像素由三个通道的数值共同决定颜色，常见 8 位图像每个通道取值为 0 到 255。

126. 问题：图像归一化是什么？

> 答案：把像素从原始范围转换到更适合模型训练的尺度，例如除以 255 得到 0 到 1，再按通道减均值、除标准差。归一化有助于训练稳定，但必须与预训练模型的要求一致。

127. 问题：数据增强是什么？

> 答案：训练时对图像执行随机裁剪、翻转、颜色变化、噪声等变换，增加数据多样性、缓解过拟合。增强不能破坏任务标签语义。

132. 问题：生成模型是什么？

> 答案：生成模型学习数据分布，并能采样生成新数据，例如图像、文本或音频。常见类型包括自回归模型、VAE、GAN 和扩散模型。

133. 问题：什么是离散 Image Token？

> 答案：图像经过离散编码器后可表示为一系列码本索引，每个索引就是离散 Image Token。生成模型可以像生成文本 Token 一样，自回归地生成这些图像 Token，再由解码器还原成图像。

134. 问题：VQ-VAE 或离散码本的基本思想是什么？

> 答案：编码器输出连续特征，再将每个特征替换为码本中最近的向量，对应一个离散索引；解码器根据量化后的向量重建图像。这样图像可转化为离散 Token 序列。

135. 问题：PSNR 评估什么？

> 答案：PSNR 基于两张对应图像的像素误差，数值越高通常表示像素差异越小。它对轻微位移敏感，不能完全代表人类视觉感受。

136. 问题：SSIM 评估什么？

> 答案：SSIM 从亮度、对比度和结构等方面比较两张图像，更关注局部结构相似性。它通常比单纯像素误差更符合视觉感知，但仍有局限。

137. 问题：FID 评估什么？

> 答案：FID 比较生成图像集合与参考图像集合在预训练特征空间中的分布差异。一般越低越好。它评估的是数据集级分布，不是单张图像与单张图像的相似度。

138. 问题：CLIP、FID、PSNR、SSIM 分别关注什么？

> 答案：CLIP：图像与文本的语义匹配；FID：两组图像的整体特征分布；PSNR：对应图像的像素误差；SSIM：对应图像的结构相似性。
> 
> 这些指标不能相互替代，通常需要组合使用。

139. 问题：鲁棒性测试中的 JPEG、噪声和模糊分别模拟什么？

> 答案：JPEG 模拟有损压缩；高斯或散斑噪声模拟像素扰动；高斯模糊和中值滤波模拟平滑或去噪处理。目的是检验水印或检测器在常见后处理后能否保持效果。

# 第十部分：对抗样本、模型鲁棒性与 AI 安全

介绍对抗鲁棒性与 AI 安全的通用基础概念。

140. 问题：什么是对抗样本？

> 答案：对原始输入添加人眼不易察觉但经过特意优化的扰动，使模型产生错误预测。对抗样本暴露了模型决策边界和鲁棒性问题。

141. 问题：白盒攻击和黑盒攻击有什么区别？

> 答案：白盒攻击者知道模型结构、参数和梯度；黑盒攻击者只能查询输出或对模型信息了解有限。迁移攻击是常见黑盒方式之一。

142. 问题：FGSM 和 PGD 有什么区别？

> 答案：FGSM 通常沿输入梯度符号一步生成扰动；PGD 在约束范围内进行多步迭代，并在每步后投影回允许范围，通常攻击更强。

143. 问题：攻击成功率 ASR 是什么？

> 答案：ASR 衡量攻击使原本正确或目标样本发生错误预测的比例。具体分母定义必须在论文中说明，例如只统计原本分类正确的样本。

144. 问题：ACC 和 ASR 为什么需要同时看？

> 答案：ACC 反映模型在正常样本上的可用性，ASR 反映攻击有效性或防御鲁棒性。只降低 ASR 但严重损失正常 ACC 的防御可能没有实际价值。

145. 问题：什么是对抗迁移性？

> 答案：在一个源模型上生成的对抗样本，也能攻击另一个目标模型。迁移性使攻击者即使不知道目标模型参数，也可能进行有效攻击。

146. 问题：什么是鲁棒训练？

> 答案：在训练过程中加入对抗样本、扰动增强或鲁棒目标，使模型在受攻击或分布变化时保持性能。鲁棒性提升常与干净准确率存在权衡。

147. 问题：TRADES 的核心思想是什么？

> 答案：TRADES 将自然分类损失与鲁棒性正则项结合，通过一个权重平衡干净准确率和对抗鲁棒性。它明确建模两者之间的权衡。

148. 问题：AWP 是什么？

> 答案：Adversarial Weight Perturbation 在训练时对模型权重施加最不利的小扰动，再优化模型，使参数附近的损失面更平坦，从而提高鲁棒性。

149. 问题：模型融合是什么？

> 答案：将多个模型的参数、输出或表示进行组合，以获得新的模型或预测。参数平均、加权融合、集成投票都属于融合方式，但适用条件不同。

150. 问题：为什么模型参数不能随便平均？

> 答案：不同模型的参数可能处于不同排列或不同优化区域，直接平均可能破坏功能。通常要求模型结构一致、初始化或训练路径具有一定兼容性，并通过实验选择融合层和比例。

151. 问题：Prompt Injection 是什么？

> 答案：攻击者在用户输入、网页或工具返回内容中嵌入恶意指令，诱导 Agent 忽略系统规则、泄露信息或调用未授权工具。

152. 问题：直接 Prompt Injection 和间接 Prompt Injection 有什么区别？

> 答案：直接注入由用户直接输入恶意指令；间接注入隐藏在外部网页、文档、邮件或工具返回中，Agent 读取后被诱导执行。

153. 问题：Agent 安全为什么不能只依赖大模型判断？

> 答案：大模型输出具有不确定性，也可能被提示注入影响。关键权限、预算、工具调用和数据访问应由独立规则、身份认证、最小权限和审计机制控制。

154. 问题：什么是最小权限原则？

> 答案：主体只获得完成当前任务所必需的最小权限，并限制作用范围和时间。即使 Agent 被攻击，也能降低可造成的损失。

155. 问题：审计日志有什么作用？

> 答案：记录谁在什么时间发起了什么请求、调用了什么工具、输入输出和处置结果，用于追责、异常分析和安全复盘。日志本身也应防篡改并控制敏感信息。

# 第十一部分：常见概念简答

下面每道题建议练习到 20—40 秒内可以完整说出。

167. 问题：请用简短语言解释 PyTorch。

> 答案：PyTorch 是一个用于张量计算和深度学习的开源框架。它的核心数据结构是 Tensor，支持 CPU 和 GPU 运算，并提供自动求导、神经网络模块、损失函数和优化器，因此可以完成从模型搭建到训练和推理的完整流程。

168. 问题：请解释 \`224×224×3\`。

> 答案：它表示一张彩色图像高 224 个像素、宽 224 个像素，并有 3 个 RGB 颜色通道。NumPy 图像通常写成 HWC，而 PyTorch 模型通常使用 CHW，加入 batch 后是 NCHW。

169. 问题：请解释 Transformer。

> 答案：Transformer 是以注意力机制为核心的序列建模架构。它通过 Q、K、V 计算不同 Token 之间的相关性，使用多头自注意力聚合信息，再通过前馈网络变换特征，并结合残差连接和 LayerNorm 稳定深层训练。

170. 问题：请解释 ViT。

> 答案：ViT 是 Transformer 在视觉任务中的应用。它把图像切成固定大小的 Patch，每个 Patch 经展平和线性映射变成 Token，加入位置编码后输入 Transformer Encoder，通过自注意力建模不同图像区域之间的关系。

171. 问题：Patch 和 Token 有什么关系？

> 答案：Patch 是图像的原始小块，Patch 经过展平和线性投影后得到的向量就是 Patch Token。Patch 是空间区域，Token 是模型处理的数值表示。

172. 问题：多头自注意力是什么？

> 答案：多头自注意力把特征分成多个子空间，每个头独立计算 Q、K、V 和注意力，使模型能够同时关注不同类型的关系。各头结果拼接后再线性映射。

173. 问题：前馈网络 FFN 是什么？

> 答案：FFN 是对每个 Token 独立应用的两层全连接网络，通常先升维、经过激活函数，再降回隐藏维度。注意力负责 Token 之间交互，FFN 负责单个 Token 内部的非线性特征变换。

174. 问题：残差连接是什么？

> 答案：残差连接把输入直接加到子层输出上，即 x 加 F(x)。它保留原始信息并为梯度提供直接通路，使深层网络更容易训练。

175. 问题：LayerNorm 是什么？

> 答案：LayerNorm 对单个样本或单个 Token 的特征维进行归一化，再使用可学习缩放和平移。它能稳定特征分布，是 Transformer 中常用的归一化方法。

176. 问题：CLIP 是什么？

> 答案：CLIP 是图文对比预训练模型，由图像编码器和文本编码器组成。它把匹配的图像和文本映射到相近位置，把不匹配图文拉远，可用于图文匹配和零样本分类。

177. 问题：矩阵转置用什么函数？

> 答案：NumPy 中可以用 A.T 或 np.transpose(A)；PyTorch 中二维张量可以用 A.T 或 A.transpose(0,1)。高维张量要根据目标维度使用 transpose 或 permute。

178. 问题：深拷贝和浅拷贝有什么区别？

> 答案：浅拷贝只复制外层，内部嵌套的可变对象仍共享；深拷贝会递归复制各层对象，因此修改深层数据通常不会影响原对象。浅拷贝可用 copy.copy()，深拷贝用 copy.deepcopy()。

179. 问题：Python 字典底层是什么？

> 答案：Python 字典底层主要是哈希表。它对 Key 计算哈希值并据此定位存储位置，因此查找、插入和删除平均复杂度通常是 O(1)。Key 必须可哈希。

# 附录：最常用 PyTorch 代码骨架

不要求死背整段代码，但要理解每一步的作用。

180. 问题：一个最基本的 PyTorch 训练循环怎么写？

> 答案：下面代码展示设备选择、前向传播、计算损失、反向传播和参数更新。
> 
> import torch
> 
> device = torch.device("cuda" if torch.cuda.is\_available() else "cpu")
> 
> model = model.to(device)
> 
> for x, y in train\_loader:
> 
> x = x.to(device)
> 
> y = y.to(device)
> 
> optimizer.zero\_grad()
> 
> output = model(x)
> 
> loss = criterion(output, y)
> 
> loss.backward()
> 
> optimizer.step()

181. 问题：验证或推理时怎么写？

> 答案：切换到评估模式，并关闭梯度记录。
> 
> model.eval()
> 
> correct = 0
> 
> with torch.no\_grad():
> 
> for x, y in val\_loader:
> 
> x = x.to(device)
> 
> y = y.to(device)
> 
> output = model(x)
> 
> pred = output.argmax(dim=1)
> 
> correct += (pred == y).sum().item()

182. 问题：常见张量形状操作怎么写？

> 答案：下面分别展示查看形状、转置、维度重排、展平和矩阵乘法。
> 
> print(x.shape) \# 查看形状
> 
> x\_t = x.transpose(0, 1) \# 交换两个维度
> 
> x\_chw = x\_hwc.permute(2, 0, 1) \# HWC -\> CHW
> 
> x\_flat = x.flatten(start\_dim=1) \# 保留 batch，后面展平
> 
> y = a @ b \# 矩阵乘法

# 知识检查清单

> □ 能解释 PyTorch 与 NumPy 的区别，并写出标准训练循环。
> 
> □ 能说明 HWC、CHW、NCHW，能计算 Patch 数量。
> 
> □ 能从 Q、K、V 开始解释注意力，并说明多头、FFN、残差、LayerNorm 的作用。
> 
> □ 能解释 ViT、Patch、Token、CLS Token、位置编码。
> 
> □ 能解释 CLIP、对比学习、零样本分类和 CLIP Score。
> 
> □ 能区分矩阵转置、求逆、矩阵乘法和逐元素乘法。
> 
> □ 能用例子解释直接赋值、浅拷贝和深拷贝。
> 
> □ 能说明字典的嵌套结构和哈希表底层实现。
> 
> □ 能解释 ACC、Precision、Recall、F1、AUC、AP、TPR、FPR。
> 
