# Python 与 AI 安全基础问答

通用技术概念速查与问答。

# Prompt Injection 与 Agent 安全

**问题：Prompt Injection通常分为哪两类？**

**答案：**最常见的是：**Direct Prompt Injection（直接提示注入）**，恶意指令直接出现在用户输入中；**Indirect Prompt Injection（间接提示注入）**，恶意指令隐藏在网页、文档、邮件或RAG检索结果等外部内容中，模型读取后被诱导执行错误操作。

**问题：Prompt Injection的主要防护思路是什么？**

**答案：**防护不能只靠Prompt，应采用分层控制：**区分系统指令和外部不可信内容、对检索内容做标记和隔离、限制工具白名单与参数、执行前二次授权、检测注入特征、最小权限、输出审计和人工复核。**核心原则是让LLM可以提出动作，但高风险动作必须由独立规则和权限模块批准。

# 对抗样本、鲁棒训练与优化器

**问题：对抗样本攻击可以怎样分类？**

**答案：**常见分类包括：**白盒攻击、黑盒攻击和物理攻击**。白盒攻击知道模型结构与参数，可直接使用梯度；黑盒攻击不知道内部参数，通常通过查询输出、迁移攻击或估计梯度；物理攻击要求扰动在拍摄、打印、光照或真实环境中仍能生效。

**问题：FGSM是什么？**

**答案：FGSM是一步梯度攻击。**它计算损失函数对输入的梯度，并沿着增加损失的符号方向给原始输入添加一次小扰动。优点是速度快，适合快速评测；缺点是攻击步骤只有一次，通常弱于迭代攻击。

**问题：BIM或I-FGSM是什么？**

**答案：BIM/I-FGSM是FGSM的迭代版本。**它不是一次加完全部扰动，而是多次沿梯度方向执行小步更新，并在每一步把结果限制到允许的扰动范围和像素范围内，因此通常比FGSM更强。

**问题：PGD是什么？**

**答案：PGD是投影梯度攻击。**它从原样本附近开始，多次沿梯度方向更新攻击样本，每次更新后都投影回规定的ε邻域内。PGD是鲁棒性评测和对抗训练中常见的强白盒攻击。

**问题：CW Attack是什么？**

**答案：CW攻击把生成对抗样本写成一个优化问题**，在尽量减小扰动的同时推动模型产生错误分类。它攻击能力强、可获得较小扰动，但计算开销通常高于FGSM和普通迭代攻击。

**问题：DeepFool是什么？**

**答案：DeepFool尝试寻找使样本跨越模型决策边界所需的较小扰动。**它通过局部近似决策边界并逐步更新样本，常用于分析模型距离决策边界有多远以及模型的局部鲁棒性。

**问题：TRADES是什么？**

**答案：TRADES是一种在正常准确率与对抗鲁棒性之间进行权衡的对抗训练方法。**它一方面要求模型在clean样本上正确分类，另一方面限制clean样本和对抗样本的预测分布差异，从而通过损失中的权重参数调节准确率和鲁棒性的平衡。

**问题：AWP是什么？**

**答案：AWP是Adversarial Weight Perturbation，对抗权重扰动。**普通对抗训练主要扰动输入，AWP还在训练时寻找会增大损失的参数小扰动，再让模型在这种不利权重变化下仍保持稳定，从而使损失区域更平坦并提升鲁棒性。

**问题：什么是优化器？**

**答案：优化器根据梯度更新模型参数，以减小损失函数。**常见优化器包括SGD、Momentum、Adam和AdamW。反向传播负责计算梯度，优化器负责决定如何利用这些梯度更新参数，两者不是同一个概念。

**问题：Adam的原理是什么？**

**答案：Adam是自适应学习率优化器。**它维护梯度的一阶矩估计，类似Momentum，用于表示历史更新方向；同时维护梯度平方的二阶矩估计，用于反映不同参数的梯度尺度。经过偏差修正后，Adam为不同参数设置不同的实际更新步长，因此通常收敛较快、训练较稳定。

**问题：学习率是什么？过大或过小有什么影响？**

**答案：学习率决定每次参数更新的步长。**学习率过大，可能越过较优区域，造成loss震荡甚至发散；学习率过小，训练收敛缓慢，可能在有限轮次内学不到有效参数。合适的学习率需要结合优化器、Batch Size、模型规模和训练阶段调整。

**问题：Warmup和学习率衰减分别有什么作用？**

**答案：Warmup是在训练初期从较小学习率逐步增大到目标值**，避免刚开始梯度不稳定导致训练发散；**学习率衰减是在训练中后期逐步减小学习率**，使参数更新更加细致，帮助模型在较优区域附近稳定收敛。

# Python 深浅拷贝与字典结构

**问题：Python中的直接赋值、浅拷贝和深拷贝有什么区别？**

**答案：直接赋值**如b=a不会创建新对象，a和b指向同一个对象；**浅拷贝**如a.copy()或copy.copy(a)会创建新的外层对象，但内部嵌套的列表、字典等可变对象仍然共享；**深拷贝**如copy.deepcopy(a)会递归复制嵌套对象，因此修改深层数据通常不会影响原对象。

**问题：什么是字典的深层次结构或嵌套字典？**

**答案：**嵌套字典是指字典的值仍然可以是**字典、列表或其他复杂对象**。例如student\['education'\]\['master'\]\['school'\]需要逐层访问。处理不确定结构时，可以使用get()并提供默认空字典，避免某个键不存在时直接抛出KeyError。

**问题：Python字典的底层结构是什么？**

**答案：Python字典底层主要基于哈希表。**系统先计算Key的哈希值，再根据哈希结果寻找存储位置，因此查找、插入和删除的平均时间复杂度通常为O(1)。Key必须是可哈希对象，例如字符串、数字和只包含可哈希元素的元组；普通列表和字典不能直接作为Key。

**问题：为什么浅拷贝在嵌套字典中容易出问题？**

**答案：**因为浅拷贝只复制最外层容器，两个字典内部可能仍指向同一个列表或子字典。修改b\['research'\]\['papers'\]时，a中的同一内部对象也会变化。只修改第一层不可变值通常互不影响；需要完全独立的嵌套数据时，应使用**copy.deepcopy()**。
